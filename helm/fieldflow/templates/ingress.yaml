apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: fieldflow-ingress
spec:
  rules:
    - host: fieldflow.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend
                port:
                  number: 3000
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: api-gateway
                port:
                  number: 3000
