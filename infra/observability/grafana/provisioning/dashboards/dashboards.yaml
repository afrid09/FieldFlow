# Purpose: Observability config: dashboards.
apiVersion: 1

providers:
  - name: default
    folder: FieldFlow
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
